{
  "name": "gsd-oc",
  "version": "0.0.0",
  "description": "GSD lifecycle orchestration for OpenClaw — native, no Claude Code.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "main": "./dist/index.js",
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && node scripts/copy-artifacts.mjs",
    "build:corpus": "node --experimental-strip-types scripts/build-corpus.ts",
    "build:vectors": "node --experimental-strip-types scripts/build-vectors.ts",
    "sync:check": "node --experimental-strip-types scripts/sync-upstream.ts --check",
    "sync:upstream": "node --experimental-strip-types scripts/sync-upstream.ts --sync",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/test/*.test.js\"",
    "health": "tsc && node scripts/copy-artifacts.mjs && node --experimental-strip-types scripts/health.ts",
    "health:strict": "node --experimental-strip-types scripts/health.ts --strict"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.30.0",
    "typebox": "^1.1.38"
  },
  "peerDependencies": {
    "openclaw": ">=2026.5.17"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "openclaw": "2026.6.8",
    "typescript": "^5.9.0"
  },
  "license": "MIT"
}
