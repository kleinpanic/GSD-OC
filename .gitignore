node_modules/
dist/
*.log
.DS_Store
.remember/

# reference substrate (kept on disk, not tracked)
.planning/research/gsd-source/
dist-test/

# RET-01 build artifact (regenerated by scripts/build-corpus.ts)

# RET-02 vector artifacts (regenerated by scripts/build-vectors.ts; shipped in dist)
src/retrieval/lancedb/

# Internal GSD planning artifacts — kept on disk, never pushed (mirrors the pre-push guard policy)
.planning/
CLAUDE.md
AGENTS.md
PROMPT.md
REPOSITORY-STRUCTURE.md

.claude/
